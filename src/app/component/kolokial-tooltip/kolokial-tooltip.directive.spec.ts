import { KolokialTooltipDirective } from './kolokial-tooltip.directive';

describe('KolokialTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new KolokialTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
